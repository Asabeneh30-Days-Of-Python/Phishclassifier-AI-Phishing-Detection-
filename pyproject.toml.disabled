[build-system]
requires = ["setuptools>=61", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "phishclassifier"
version = "0.0.0"
description = "PhishClassifier placeholder"
authors = [{ name = "you" }]
